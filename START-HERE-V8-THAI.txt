TUM PA GUAY RESTAURANT V8.0

1) อัปโหลดไฟล์ V8 ทั้งหมดขึ้น GitHub Pages โดยให้ index.html อยู่ที่ root
2) เข้า Supabase > SQL Editor > New query
3) เปิดไฟล์ V8-UPGRADE.sql คัดลอกทั้งหมด แล้วกด Run หนึ่งครั้ง
4) เปิด pos.html สำหรับขายหน้าร้าน
5) เปิด reports.html สำหรับรายงาน ค้นหาบิล Export CSV และ Backup JSON
6) บน iPhone/iPad เปิด Safari > Share > Add to Home Screen เพื่อติดตั้งเป็น PWA

V8 ไม่ลบประวัติเดิม และใช้ฐานข้อมูลเดียวกับ V7.1
ห้ามใส่ Secret key หรือ Service Role key ใน GitHub
